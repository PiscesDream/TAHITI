#include <program_header.h>
#include <system.h>
#include <stdio.h>

int main() {
//    int pid = fork();
    printf("hi %d %d %d\n", 1, 2, 3);
    while (1) {
        char c = getch();
//      printf("input: int, char, string\n");
//      int a;
//      char b;
//      char c[10];
//      scanf("%d %c %s", &a, &b, c);
//      printf("int: %d, char: %c, string: %s\n", a, b, c);
    }
}
